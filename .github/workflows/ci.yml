name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r store/requirements.txt

      - name: Run Django tests
        env:
          DJANGO_DB_ENGINE: sqlite
        working-directory: store
        run: |
          python manage.py test --settings=store_service.settings_test

  frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Inject environment
        run: |
          echo "NEXT_PUBLIC_BACKEND_URL=${{ secrets.BACKEND_URL_PROD }}" >> frontend/.env.production

      - name: Install dependencies
        run: |
          if [ ! -f frontend/package.json ]; then
            echo "frontend/package.json not found, skipping Node.js dependency install."
            exit 0
          fi
          cd frontend
          npm install

      - name: Build Next.js frontend
        run: |
          if [ ! -f frontend/package.json ]; then
            echo "frontend/package.json not found, skipping Next.js build."
            exit 0
          fi
          cd frontend
          npm run build

